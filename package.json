{
  "dependencies": {
    "playwright": "^1.58.2"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.7.0",
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0",
    "cucumber-html-reporter": "^7.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.2"
  },
  "scripts": {
    "test": "npm run build && npx cucumber-js && node reports-config.js",
    "test:report": "npm run build && npx cucumber-js && node reports-config.js",
    "build": "npx tsc"
  }
}
